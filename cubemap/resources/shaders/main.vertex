#version 130

in vec3 position;
in vec2 texcoord;

uniform mat4 model;
uniform mat4 projection;

out vec2 Texcoord;

void main()
{
	Texcoord = texcoord;

	gl_Position = projection * model * vec4(position, 1.0);
}
